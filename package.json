{
  "name": "ai-powered-local-business-helper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node server/index.js"
  }
}
